Position: aboslute removes an element from the normal flow. It's perfect for a layout like this since it allows me to stack elements in the same spot. If I remove it from " #magazine-cover h2::before  " , the elements begin to stack on top of each other messing up the layout .

It inserts content before the real content of an element and it can be more useful than adding an entirely new element to fit behind something.

I was struggling on the before section since I couldnt align the elements the way I wanted to but using DevTools helped me realize it was a lot simpler than I was making it.